<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Abaikan folder OJS (ganti "jurnal" dengan nama foldermu jika berbeda)
    RewriteCond %{REQUEST_URI} !^/jurnal/
    
    # Arahkan semua sisa traffic lainnya ke folder public Laravel
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>